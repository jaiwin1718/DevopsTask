# Copy this file to ".env" and adjust values as needed.
# These environment variables configure the app and the database connection.

# App
PORT=3000

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=learningdemo
